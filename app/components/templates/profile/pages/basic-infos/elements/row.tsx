import React from "react";

const Row = ({ label, value }: { label: string; value?: string }) => {
  return <div className="flex-between">
    <div>
        <p></p>
        <p></p>
    </div>
    <button className="text-primary-700 cursor-pointer">

    </button>
  </div>;
};

export default Row;
