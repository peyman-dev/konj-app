import React from "react";

const DownloadTitle = ({ title }: { title: string }) => {
  return (
    <div>
      <p className="font-YekanBakh-Regular text-[#161616]">{title} :</p>
    </div>
  );
};

export default DownloadTitle;
